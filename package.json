{
  "name": "tsnode1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install && npx --package typescript tsc",
    "start": "npm run build && node build/index.js",
    "clean": "npm install -D typescript && npx --package typescript tsc clean-all.ts && node clean-all.js"
  },
  "author": "Matt | Varghese Mathew",
  "license": "GPL v3",
  "devDependencies": {
    "@types/node": "^17.0.10",
    "typescript": "^4.5.5"
  }
}